import { createStubEngine } from "../types";

/** Report Engine — placeholder. Real implementation to be added later. */
export const reportEngine = createStubEngine({
  id: "report",
  name: "Report Engine",
  description: "Assembles the engineering report from all engine outputs.",
});
