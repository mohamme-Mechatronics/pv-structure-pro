import { createStubEngine } from "../types";

/** Load Engine — placeholder. Real implementation to be added later. */
export const loadEngine = createStubEngine({
  id: "load",
  name: "Load Engine",
  description: "Computes dead, wind and combined load cases per ASCE 7-22.",
});
