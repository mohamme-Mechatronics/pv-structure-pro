import { createStubEngine } from "../types";

/** Array / Layout Engine — placeholder. Real implementation to be added later. */
export const arrayLayoutEngine = createStubEngine({
  id: "arrayLayout",
  name: "Array / Layout Engine",
  description: "Generates feasible PV array configurations for the available area.",
});
