import { createStubEngine } from "../types";

/** Base Plate Engine — placeholder. Real implementation to be added later. */
export const basePlateEngine = createStubEngine({
  id: "basePlate",
  name: "Base Plate Engine",
  description: "Designs column base plates.",
});
