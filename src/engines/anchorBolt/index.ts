import { createStubEngine } from "../types";

/** Anchor Bolt Engine — placeholder. Real implementation to be added later. */
export const anchorBoltEngine = createStubEngine({
  id: "anchorBolt",
  name: "Anchor Bolt Engine",
  description: "Designs anchor bolt groups for column bases.",
});
