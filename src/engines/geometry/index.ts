import { createStubEngine } from "../types";

/** Geometry Engine — placeholder. Real implementation to be added later. */
export const geometryEngine = createStubEngine({
  id: "geometry",
  name: "Geometry Engine",
  description: "Derives table, column, rafter and purlin geometry from the selected layout.",
});
