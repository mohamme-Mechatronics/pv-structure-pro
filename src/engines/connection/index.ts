import { createStubEngine } from "../types";

/** Connection Engine — placeholder. Real implementation to be added later. */
export const connectionEngine = createStubEngine({
  id: "connection",
  name: "Connection Engine",
  description: "Designs bolted and welded member connections.",
});
