import { createStubEngine } from "../types";

/** Panel Library Engine — placeholder. Real implementation to be added later. */
export const panelLibraryEngine = createStubEngine({
  id: "panelLibrary",
  name: "Panel Library Engine",
  description: "Resolves PV module specifications from the centralized panel library.",
});
