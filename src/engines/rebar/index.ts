import { createStubEngine } from "../types";

/** Rebar Engine — placeholder. Real implementation to be added later. */
export const rebarEngine = createStubEngine({
  id: "rebar",
  name: "Rebar Engine",
  description: "Designs footing reinforcement (B500).",
});
