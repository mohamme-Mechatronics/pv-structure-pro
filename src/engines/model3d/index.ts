import { createStubEngine } from "../types";

/** 3D Engine — placeholder. Real implementation to be added later. */
export const model3dEngine = createStubEngine({
  id: "model3d",
  name: "3D Engine",
  description: "Builds the parametric 3D model for the viewer.",
});
