import { createStubEngine } from "../types";

/** Input Engine — placeholder. Real implementation to be added later. */
export const inputEngine = createStubEngine({
  id: "input",
  name: "Input Engine",
  description: "Validates and normalises project inputs into the parametric model.",
});
