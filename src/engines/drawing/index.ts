import { createStubEngine } from "../types";

/** Drawing Engine — placeholder. Real implementation to be added later. */
export const drawingEngine = createStubEngine({
  id: "drawing",
  name: "Drawing Engine",
  description: "Generates A3 drawing sheets from the model.",
});
