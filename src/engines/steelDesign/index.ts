import { createStubEngine } from "../types";

/** Steel Design Engine — placeholder. Real implementation to be added later. */
export const steelDesignEngine = createStubEngine({
  id: "steelDesign",
  name: "Steel Design Engine",
  description: "Sizes columns, rafters and purlins per AISC 360-22.",
});
