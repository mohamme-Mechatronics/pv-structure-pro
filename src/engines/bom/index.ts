import { createStubEngine } from "../types";

/** BOM Engine — placeholder. Real implementation to be added later. */
export const bomEngine = createStubEngine({
  id: "bom",
  name: "BOM Engine",
  description: "Quantifies materials into a bill of materials.",
});
